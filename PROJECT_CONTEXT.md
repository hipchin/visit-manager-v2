# PROJECT_CONTEXT.md

## 目的

このドキュメントは、`visit-manager-v2` の今後の開発・修正・レビュー時に毎回参照するための設計メモである。

コードそのものではなく、アプリの目的、構造、データ設計、画面構成、更新時の注意点を整理する。

今後このリポジトリを修正する場合は、まずこのファイルを読み、既存設計を壊さないことを優先する。

---

## リポジトリ情報

- GitHub フルネーム: `hipchin/visit-manager-v2`
- デフォルトブランチ: `main`
- アプリ名: 訪問管理
- 形式: 静的HTML/CSS/JavaScriptで構成されたPWA
- 主な利用想定: iPhoneでホーム画面に追加して使う訪問先管理アプリ
- デプロイ想定: GitHub Pagesなどの静的ホスティング

---

## アプリの目的

このアプリは、訪問先を記録し、再訪問やフォローをしやすくするための個人用訪問管理アプリである。

主な目的は次の通り。

1. 訪問先の場所・住所・名前・性別・メモを保存する
2. 最終訪問日と会えた時間帯を記録する
3. 訪問履歴と不在履歴を残す
4. タグで訪問先を分類する
5. 古くなった訪問先を一覧で把握しやすくする
6. 住所または座標からGoogleマップを開けるようにする
7. データをJSONでバックアップ・復元できるようにする

---

## 技術構成

### 基本構成

このアプリはビルド工程を持たない、素の静的Webアプリである。

- `index.html`: 画面構造、PWAメタ情報、外部ライブラリ読み込み、各JSファイル読み込み
- `css/style.css`: 画面全体のスタイル、iPhone利用を前提にしたUI調整
- `js/db.js`: データ永続化レイヤー
- `js/tags.js`: タグ色、日付、時間帯などの共通ユーティリティ
- `js/ui.js`: 画面描画、フォーム操作、モーダル、位置情報、地図ピッカー
- `js/app.js`: 初期化、イベントバインド、メインロジック、Service Worker登録
- `manifest.json`: PWA設定
- `sw.js`: Service Worker
- `icons/`: PWAアイコン

### 外部ライブラリ

- Leaflet `1.9.4`
  - 地図ピッカー表示に使用
- OpenStreetMap タイル
  - Leaflet地図の背景地図として使用
- Nominatim reverse geocoding API
  - 現在地または地図ピンから住所候補を取得するために使用

---

## 画面構成

### 1. リスト画面

HTML上の主なID:

- `screen-list`
- `visit-list`
- `search-input`
- `filter-bar`
- `filter-tags-list`
- `btn-filter`
- `btn-filter-clear`
- `btn-trash-nav`
- `btn-settings-nav`
- `btn-add`
- `backup-notice`

役割:

- 訪問先カードを一覧表示する
- 検索キーワードで絞り込む
- タグフィルターで絞り込む
- 古い訪問先を警告表示する
- カードのスワイプで削除ボタンを表示する
- カード内のマップボタンからGoogleマップを開く
- ゴミ箱画面と設定画面へ遷移する
- 新規登録画面へ遷移する

並び順:

- 最終訪問日が古い順
- 最終訪問日がないものは古い扱いになる

注意点:

- 一覧は軽さが重要
- 将来データ件数が増えた場合、一覧描画コストが起動速度に影響する可能性がある
- 検索・フィルターは現在メモリ上の配列に対して実行している

---

### 2. 詳細・編集画面

HTML上の主なID:

- `screen-detail`
- `field-id`
- `field-address`
- `field-display-title`
- `field-place-memo`
- `field-lat`
- `field-lng`
- `field-location-source`
- `field-name`
- `field-gender`
- `field-last-visit`
- `field-memo`
- `time-options`
- `selected-tags`
- `visit-history-section`
- `visit-history-list`
- `btn-save`
- `btn-delete-entry`
- `btn-map-preview`
- `btn-use-current-location`
- `btn-open-location-picker`

役割:

- 訪問先の新規登録
- 訪問先の編集
- 訪問履歴の追加・編集・削除
- 不在履歴の追加・編集・削除
- 現在地から住所・座標を保存
- 地図ピッカーから住所・座標を保存
- 座標または住所を使ってGoogleマップを開く

保存条件:

- `address` または `displayTitle` のどちらかが必要
- `address` が空でも `displayTitle` があれば保存できる

注意点:

- `field-lat` と `field-lng` は画面上には表示されないが、地図連携に重要
- 住所を手入力した場合、既存座標とのズレを避けるため座標をクリアする設計になっている
- 訪問履歴を更新すると、最終訪問日と時間帯が履歴から同期される
- 新規登録時は訪問履歴セクションを表示しない

---

### 3. ゴミ箱画面

HTML上の主なID:

- `screen-trash`
- `trash-list`
- `btn-back-trash`

役割:

- 削除した訪問先を表示する
- 削除から30日以内の訪問先を復元できる
- 30日を過ぎたものは起動時に整理される

注意点:

- 削除は即時完全削除ではなく、まず `trash` に移動する
- `deletedAt` を基準に30日経過判定をする

---

### 4. 設定画面

HTML上の主なID:

- `screen-settings`
- `tags-list`
- `btn-add-tag`
- `btn-export`
- `btn-import`
- `import-file`
- `btn-update`
- `backup-last-text`
- `version-text`

役割:

- タグの追加・編集・削除
- JSONバックアップのエクスポート
- JSONバックアップからの復元
- アプリの手動更新
- バージョン表示

注意点:

- インポートは現在データを上書きする
- バックアップ未実施または最終バックアップから30日以上経過した場合、リスト画面に通知を出す
- 手動更新はService Worker登録がある場合に動作する

---

## モーダル構成

### タグ選択モーダル

- `modal-tag-picker`
- `tag-picker-list`

役割:

- 訪問先に付けるタグを選択する

### タグ追加・編集モーダル

- `modal-add-tag`
- `new-tag-name`
- `color-picker`

役割:

- タグ名と色を設定する

### 訪問記録モーダル

- `modal-visit`
- `visit-date`
- `visit-time-options`
- `visit-memo`

役割:

- 会えた訪問を履歴に追加・編集する

### 不在記録モーダル

- `modal-absent`
- `absent-date`
- `absent-memo`

役割:

- 不在訪問を履歴に追加・編集する

### 場所選択モーダル

- `modal-location-picker`
- `location-map`
- `location-picker-status`

役割:

- Leaflet地図から訪問先の座標を選ぶ
- ピンをドラッグまたは地図タップで位置を決める
- 住所候補を取得できれば住所も保存する
- 住所候補の取得に失敗しても座標は保存できる

### 確認ダイアログ

- `modal-confirm`
- `confirm-message`

役割:

- ブラウザ標準の `confirm()` ではなく、アプリ内UIとして確認を出す
- 削除、上書きインポート、履歴削除などで使用する

---

## データ保存設計

### メイン保存先

メインの永続化には `localStorage` を使う。

主なキー:

- `vm_visits`: 訪問先データ
- `vm_trash`: ゴミ箱データ
- `vm_tags`: タグデータ
- `vm_backup_date`: 最終バックアップ日時
- `vm_version`: バージョン用キー。ただし現状では主に `APP_VERSION` が使われている

### IndexedDB

IndexedDBはメイン保存先ではない。

役割:

- 起動時に現在データのスナップショットを保存する
- DB名: `visit_manager_snap`
- object store: `snapshots`
- key: `latest`

注意点:

- IndexedDBは復元UIには直結していない
- ユーザー操作上のバックアップはJSONエクスポートが主手段
- 起動時の保険としてのスナップショットであり、通常の読み書きはlocalStorageが中心

---

## 訪問先データの概念モデル

訪問先データは、おおむね次の情報を持つ。

- `id`: 訪問先ID
- `address`: 場所・住所
- `displayTitle`: 一覧表示名
- `placeMemo`: 場所メモ
- `lat`: 緯度
- `lng`: 経度
- `locationSource`: 座標取得元
- `name`: 名前
- `gender`: 性別
- `lastVisit`: 最終訪問日
- `lastTime`: 最終訪問の時間帯
- `memo`: メモ
- `tags`: タグID配列
- `visitHistory`: 訪問・不在履歴配列
- `createdAt`: 作成日時
- `updatedAt`: 更新日時
- `deletedAt`: ゴミ箱移動日時

### `locationSource` の想定値

- `current`: 現在地から取得
- `pin`: 地図ピッカーから取得
- 空文字: 座標なし、または取得元未設定

### `gender` の想定値

- `male`: 男性
- `female`: 女性
- `other`: その他
- 空文字: 未設定

### `lastTime` と履歴の `time` の想定値

- `morning`: 午前
- `afternoon`: 午後
- `evening`: 夕方
- `night`: 夜
- 空文字: 未設定

---

## 訪問履歴データの概念モデル

訪問履歴は `visitHistory` 配列として訪問先に保持される。

会えた履歴:

- `date`: 訪問日
- `time`: 時間帯
- `memo`: メモ

不在履歴:

- `date`: 訪問日
- `absent`: `true`
- `memo`: メモ

設計上の扱い:

- 訪問履歴は日付の新しい順に表示される
- 同日データは後から追加されたものを上に表示する
- 会えた履歴には「何回目」バッジを表示する
- 不在履歴は「不在」として区別する
- 最終訪問日は履歴の最新日付から同期される
- 最新履歴が不在の場合、最終訪問日は更新されるが時間帯チップは選択されない

---

## タグ設計

タグは `vm_tags` に保存される。

初期タグ:

- エリア
- 手応えあり
- 要フォロー

タグデータ:

- `id`: タグID
- `name`: タグ名
- `color`: 色コード

タグ色候補:

- 青 `#2563eb`
- 緑 `#16a34a`
- オレンジ `#d97706`
- 赤 `#dc2626`
- 紫 `#9333ea`
- シアン `#0891b2`
- ピンク `#be185d`
- グレー `#737373`

注意点:

- タグ削除時は、訪問先データ内の該当タグIDも削除される
- タグ名変更では訪問先側のタグIDは変えない
- 表示はタグIDをもとにタグマスタから名前と色を引く

---

## 位置情報・地図設計

### 基本方針

場所情報は、住所文字列だけではなく座標も保存できる。

Googleマップを開く際は、座標がある場合は座標を優先し、座標がない場合は住所文字列を使う。

### 現在地入力

- ブラウザのGeolocation APIを使う
- 高精度設定を有効にして現在地を取得する
- 取得した緯度経度をNominatimで逆ジオコーディングする
- 住所候補が取得できた場合は住所と座標を保存する
- 住所候補が取得できない場合も座標だけは保存する

### 地図ピッカー

- Leafletで地図を表示する
- 初期位置は既存座標があればその座標を使う
- 既存座標がなければ横浜周辺の座標を使う
- 地図タップまたはマーカードラッグで座標を選ぶ
- 選択座標から住所候補を取得する
- 住所候補が取れない場合でも座標は保存できる

### 重要な注意点

- 住所を手入力した場合は、保存済み座標を消す
- これは「住所だけ変更したのに古い座標でナビが開く」事故を防ぐための設計である
- 座標がある場合、Googleマップは住所ではなく座標で開く
- 住所表記の精度よりも、現地で迷わないことを優先する設計である

---

## バックアップ設計

### エクスポート

エクスポート時にJSONファイルを生成する。

ファイル名:

- `visit-backup-YYYY-MM-DD.json`

エクスポート対象:

- `version`
- `exportedAt`
- `visits`
- `trash`
- `tags`

### インポート

インポート時はJSONファイルを読み込み、現在のデータを上書きする。

注意点:

- マージではなく上書き
- `visits` が配列でない場合は不正データとして扱う
- `trash` がない場合は空配列にする
- `tags` が配列の場合のみタグも復元する
- インポート前に確認ダイアログを出す

### バックアップ通知

- バックアップ履歴がない場合は通知を表示する
- 最終バックアップから30日以上経過した場合も通知を表示する

---

## PWA設計

### manifest

- アプリ名: 訪問管理
- 表示モード: `standalone`
- 背景色: 白
- テーマ色: 白
- 言語: 日本語
- アイコン: `icons/icon-192.png`, `icons/icon-512.png`

### iPhone向け設定

`index.html` 側で次の設定を持つ。

- `apple-mobile-web-app-capable`
- `apple-mobile-web-app-status-bar-style`
- `apple-mobile-web-app-title`
- `apple-touch-icon`
- `viewport` は拡大禁止寄りの設定

注意点:

- iPhoneホーム画面追加時の挙動を壊さないこと
- アイコン変更時は `manifest.json` と `apple-touch-icon` の両方を確認すること
- CSS変更時は `100dvh`、スクロール、固定ヘッダー、下部余白への影響を確認すること

---

## Service Worker設計

現在のService Workerは、オフライン優先ではなく更新優先である。

設計方針:

- インストール時に即時有効化する
- アクティベート時に旧キャッシュを削除する
- fetchイベントではキャッシュ応答しない
- GitHub Pages上の最新版確認を優先する

注意点:

- `CACHE_VERSION` を更新すると旧キャッシュ削除対象が変わる
- `index.html` 側のCSS/JS読み込みURLにはクエリ文字列でバージョンが付いている
- PWAで更新が反映されない問題を避けるため、キャッシュ設計を安易に変えないこと
- 将来オフライン対応を強化する場合は、更新反映の遅れとトレードオフになる

---

## 初期化設計

`app.js` の初期化では、ボタンが無反応になる事故を避けるため、イベント登録を最優先している。

初期化順の考え方:

1. イベント登録
2. 期限切れゴミ箱の整理
3. IndexedDBへのスナップショット
4. バックアップ通知更新
5. 旧デモデータ整理
6. サンプルデータ投入
7. 一覧描画
8. Service Worker登録

注意点:

- 起動時の一部処理が失敗しても、全体が止まらないよう `safeRun` で囲まれている
- `?debug=1` を付けるとiPhone Safari上でも起動時エラーを画面表示できる
- 起動トラブル時は、まず `?debug=1` を付けて確認する

---

## UI設計方針

### 全体方針

- iPhoneで片手操作しやすいことを優先する
- シンプルなカード型リストを中心にする
- 画面遷移は疑似画面切り替えで行う
- ページ遷移は使わない
- モーダルは同一HTML内に定義して表示・非表示を切り替える

### スクロール設計

- `.screen` は `height: 100dvh` で画面高に合わせる
- `.list-container` などの内部コンテナだけをスクロールさせる
- `overscroll-behavior: none` を設定して余計な揺れを抑える

### 削除UI

- 一覧カードは左スワイプで削除ボタンを出す
- 削除はゴミ箱移動であり、即時完全削除ではない

### 警告表示

- 最終訪問日がない、または14日以上経過している訪問先は警告寄りに表示する
- 最終訪問から3日以内は良好表示にする

---

## 現在の重要な設計判断

### 1. localStorage中心

現状はlocalStorageが主保存先である。

メリット:

- 実装が単純
- 静的PWAと相性がよい
- サーバー不要

懸念:

- データ件数が増えると読み書きとJSONパースが重くなる
- 容量制限がある
- 端末・ブラウザ単位の保存であり、クラウド同期はない

将来の代替案:

- IndexedDBを主保存先にする
- 集計キャッシュを持つ
- 一覧描画を仮想リスト化する
- バックアップと復元の導線を強化する

### 2. Service Workerは更新優先

現状はオフライン完全対応より、最新版が反映されやすいことを優先している。

メリット:

- 修正後に古いファイルが残る事故を減らせる
- GitHub Pages運用で更新確認しやすい

懸念:

- オフライン時の利用保証は弱い

### 3. 座標優先の地図起動

座標がある場合は、住所ではなく座標でGoogleマップを開く。

メリット:

- 住所が曖昧な場所でも現地に近い地点を開ける
- 地図ピンで微調整した情報を活かせる

懸念:

- 住所を変更した際に古い座標が残ると危険

対策:

- 住所手入力時は座標を消す

---

## 今後の開発で守ること

### 絶対に守ること

- 既存データ構造を壊さない
- localStorageキー名を変更しない
- `id` の扱いを変えない
- `visitHistory` の構造を勝手に変更しない
- `lat` と `lng` の既存挙動を壊さない
- PWAアイコン、manifest、Service Workerの関係を壊さない
- iPhoneでの表示崩れ、スクロール揺れ、ボタン無反応を最優先で避ける
- `?debug=1` のエラー表示機能を壊さない

### コード修正前に確認すること

1. どの画面に関係する変更か
2. データ保存に影響するか
3. 既存データの移行が必要か
4. iPhone PWAで表示や操作に影響するか
5. Service Workerやキャッシュ更新に影響するか
6. バックアップJSONの互換性に影響するか
7. 位置情報や地図起動の挙動に影響するか

### 変更後に確認すること

- 新規登録できるか
- 既存データを編集できるか
- 訪問履歴を追加・編集・削除できるか
- 不在履歴を追加・編集・削除できるか
- タグを追加・編集・削除できるか
- 検索できるか
- タグフィルターできるか
- 削除してゴミ箱へ移動できるか
- ゴミ箱から復元できるか
- バックアップをエクスポートできるか
- バックアップから復元できるか
- 現在地入力が壊れていないか
- 地図ピッカーが壊れていないか
- Googleマップを開けるか
- iPhoneのホーム画面起動で白画面にならないか
- `?debug=1` でエラー表示が機能するか

---

## 将来の改善候補

### データ増加対策

- localStorage全件読み込みの見直し
- IndexedDBを主保存先にする
- 一覧用の軽量インデックスを持つ
- 最終訪問日やタグ別の集計キャッシュを持つ
- 一覧描画を必要分だけにする

### バックアップ改善

- インポート前に件数確認を出す
- 上書きではなくマージ復元を選べるようにする
- 自動バックアップの案内を強化する
- IndexedDBスナップショットから復旧するUIを追加する

### 地図改善

- 登録済み訪問先を地図上に一覧表示する
- 近い訪問先を探せるようにする
- 住所がない場所では座標表示を明示する
- 座標の手入力欄を追加する

### UI改善

- リストの表示密度調整
- 訪問履歴の視認性改善
- タグフィルターの状態保存
- 並び順の切り替え
- 長期未訪問のみ表示

### PWA改善

- 更新がある場合の通知
- オフライン時の表示改善
- アイコンやスプラッシュ表示の調整

---

## 開発時の基本方針

このアプリは、複雑な業務システムではなく、個人がiPhoneで素早く使うための実用アプリである。

したがって、今後の開発では次の順番で優先する。

1. データが消えないこと
2. iPhoneで確実に動くこと
3. 起動が軽いこと
4. 操作が直感的であること
5. 既存UIを大きく変えないこと
6. 仕組みを複雑にしすぎないこと
7. 将来のデータ増加に備えること

便利そうな機能でも、起動速度・データ安全性・iPhone操作性を犠牲にする場合は慎重に扱う。

---

## AIアシスタント向け作業ルール

このリポジトリを扱うAIアシスタントは、次の手順を守る。

1. まず `PROJECT_CONTEXT.md` を読む
2. 次に関係する既存ファイルを読む
3. 現状の設計に沿った修正案を出す
4. 変更範囲を最小限にする
5. 既存データとの互換性を確認する
6. iPhone PWAへの影響を確認する
7. 修正後は変更ファイル、変更内容、確認方法を説明する

禁止事項:

- 未確認のまま大規模リファクタリングしない
- 既存UIを勝手に作り替えない
- データ構造を勝手に変更しない
- Service Workerやmanifestを軽く扱わない
- 位置情報・座標・住所の関係を雑に変更しない
- 「動きそう」だけで完了扱いにしない

---

## 現時点で不明なこと

次の点は、現時点のコード確認だけでは確定できない。

- 実際のGitHub Pages公開URL
- 利用端末ごとのSafari/PWA挙動差
- 実データ件数が増えた場合の具体的な起動時間
- Nominatim利用制限に対する実運用上の影響
- IndexedDBスナップショットから復旧する運用手順

これらは今後、実機確認または運用しながら検証する。
